/**
 * Web layer. REST controllers that accept HTTP requests (routed by the
 * DispatcherServlet front controller), validate input, and delegate to services.
 */
package com.cmpe172.rental.controller;
