/**
 * Data transfer objects returned by the API. Kept separate from the internal
 * {@code model} types so the wire format can evolve independently of the schema.
 */
package com.cmpe172.rental.dto;
