# Copy to .env and adjust for your environment. Do NOT commit a real .env.
# These match the docker-compose.yml dev defaults.
DB_URL=jdbc:postgresql://localhost:5432/rental
DB_USERNAME=rental
DB_PASSWORD=rental
